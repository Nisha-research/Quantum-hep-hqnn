# Streamlit Cloud optimized dependencies
# Pinned versions to avoid conflicts and installation errors

streamlit>=1.35.0
matplotlib>=3.8.0
numpy>=1.24.0,<2.0.0
scipy>=1.11.0
scikit-learn>=1.3.0
pillow>=10.0.0
pandas>=2.0.0

# TensorFlow - CPU only (smaller, faster install)
tensorflow-cpu>=2.13.0,<2.15.0

# Quantum computing - compatible with Streamlit Cloud
pennylane>=0.33.0,<0.34.0
pennylane-qiskit>=0.33.0

# Optional: Uncomment if needed for IBM Quantum
# qiskit>=1.0.0
# qiskit-ibm-runtime>=0.15.0

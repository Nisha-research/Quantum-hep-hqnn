# Core dependencies - optimized for Railway free tier (512MB RAM limit)
matplotlib>=3.8.0,<3.9.0
numpy>=1.24.0,<2.0.0
scikit-learn>=1.3.0,<1.4.0
streamlit>=1.28.0,<1.30.0

# Quantum computing - lightweight version
pennylane>=0.33.0,<0.46.0
pennylane-qiskit>=0.33.0

# ML - CPU only (no CUDA overhead)
tensorflow-cpu>=2.13.0,<2.14.0

# Optional: IBM Quantum (comment out if memory constrained)
# qiskit>=1.0.0,<1.1.0
# qiskit-ibm-runtime>=0.15.0,<0.16.0

# Utilities
pillow>=10.0.0
scipy>=1.11.0,<1.12.0
